#!/usr/bin/env python3
"""Deprecated. See ./aw-workspace-cli — kept only as a pointer so old muscle
memory (and LLMs pattern-matching on the monolith's ./aw) don't silently
land here and get confused."""
print("./aw is deprecated inside aw-workspace — use 'aw-workspace-cli' instead (it's on PATH).")
