fastapi==0.135.2
uvicorn==0.42.0
# uvicorn's WebSocket protocol impl — NOT bundled by plain `uvicorn` (only by
# `uvicorn[standard]`). Without it uvicorn can't upgrade WS requests and every
# websocket route (/ws/terminal, /ws/status) 404s while HTTP works — mirrors
# the monolith's aw.requirements.txt (plain uvicorn + explicit websockets).
websockets==16.0
sqlmodel==0.0.38
SQLAlchemy==2.0.49
psycopg[binary]==3.2.3
PyJWT==2.12.1
cryptography==46.0.6
# Password hashing for scoped guest users (src/api/guest_users.py). Present in
# existing venvs only incidentally — it was never a declared dependency, so
# importing it without this line works today and breaks on the next image
# build. src/start/workspace.py reinstalls the venv when this file's hash
# changes, so a plain restart picks it up too.
bcrypt==5.0.0
httpx[http2]==0.28.1
# Tier-2 (container) apps: the docker SDK speaks the Docker API to the host's
# rootless podman socket (AW_CONTAINER_SOCKET). Imported lazily — only apps with
# tier=container pull it in.
docker==7.1.0
python-multipart==0.0.20
redis==8.0.1
pytest==9.0.2
# This process's own OTel export — see src/api/otel.py. Set pinned/proved
# together (1.40.0 SDK line, 0.61b0 instrumentation line, matching the
# monolith's own working config). No RequestsInstrumentor: this codebase
# only calls out over httpx, and NOT instrumenting requests is what keeps
# the OTLP exporters below (built on requests) from tracing their own
# outbound export call.
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-http==1.40.0
opentelemetry-instrumentation-asgi==0.61b0
opentelemetry-instrumentation-httpx==0.61b0
